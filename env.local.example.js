// CivicSays — local env override (development only)
//
// The committed js/config.js is the default for everyone cloning the repo.
// This file is an OPTIONAL override — copy to `env.local.js` (no `.example`)
// and add <script src="env.local.js"></script> BEFORE <script src="js/config.js"></script>
// to use a different Supabase project for local testing.
//
// SECURITY: `env.local.js` is gitignored. Never commit it.
